#!/bin/sh
# nexa-claims — bare command for scripts/verify-claims.mjs, added to PATH while this plugin is enabled.
# Resolves relative to itself so it works from the plugin cache and from a clone alike.
exec node "$(dirname "$0")/../scripts/verify-claims.mjs" "$@"
