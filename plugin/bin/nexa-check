#!/bin/sh
# nexa-check — bare command for scripts/check.mjs, added to PATH while this plugin is enabled.
# Resolves relative to itself so it works from the plugin cache and from a clone alike.
exec node "$(dirname "$0")/../scripts/check.mjs" "$@"
