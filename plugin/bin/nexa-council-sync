#!/bin/sh
# nexa-council-sync — bare command for scripts/council-sync.mjs, added to PATH while this plugin is enabled.
# Resolves relative to itself so it works from the plugin cache and from a clone alike.
exec node "$(dirname "$0")/../scripts/council-sync.mjs" "$@"
