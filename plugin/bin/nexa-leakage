#!/bin/sh
# nexa-leakage — bare command for scripts/no-product-leakage.mjs, added to PATH while this plugin is enabled.
# Resolves relative to itself so it works from the plugin cache and from a clone alike.
exec node "$(dirname "$0")/../scripts/no-product-leakage.mjs" "$@"
