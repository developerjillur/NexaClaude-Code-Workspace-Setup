#!/bin/sh
# nexa-audit — bare command for scripts/kill-audit.mjs, added to PATH while this plugin is enabled.
# Resolves relative to itself so it works from the plugin cache and from a clone alike.
exec node "$(dirname "$0")/../scripts/kill-audit.mjs" "$@"
